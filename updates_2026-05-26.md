# Slide review — pending updates

## Must fix (will break compilation or cause visible errors)

- **LaTeX image path bug** (slide 4, line 60): `\graphicspath{ {img/} }` is already set, so `\includegraphics` path must be `doi_item_type_2023.png` not `img/doi_item_type_2023.png` — otherwise LaTeX looks for `img/img/doi_item_type_2023.png` and fails.
- **ORCID date inconsistency**: slide 2 timeline says 2015, slide 5 says 2014. The ETD2021 proposal confirms 2014 is correct — fix slide 2.
- **Spelling**: line 17, slide 2 — "shoulder" should be "shoulders" (Newton's quote uses plural).

## Content improvements

- **Slide 3 (DOI Minter)**: uncomment the line `%This architectural choice is \emph{why} everything else was possible.` — it is the payoff of the slide.
- **Slide 4 (DOIs by type)**: remove the leftover `% TODO` comment (lines 65–66) since the image is now in place. Also drop the `\figure`/`\caption` environment — Beamer renders captions poorly; use `\includegraphics` directly.
- **Slide 6 (ROR)**: add a sentence on why ROR matters — machine-readable affiliation makes KAUST outputs discoverable as an institution in DataCite Commons and beyond.
- **Slide 7 (Relationships)**: add the actual numbers — 19 datasets with `IsSupplementTo`, 5 theses with `IsSupplementedBy` — to make the workflow feel concrete.
- **Slide 8 (Unfinished project)**: still missing the DataCite Commons screenshot of `10.25781/kaust-ih30e` showing 52 navigable references — this is the most visually compelling moment in the talk.

## Timing / structure

- **Slide 3 (DOI Minter)** is the most at-risk slide if time is tight. Can be cut or absorbed into slide 2 if needed.
- **Slide 5 (ORCID)** is the fullest slide — if trimming is needed, drop the advisor/committee search bullet.

## Before the talk

- Verify the docling URL (`https://www.docling.ai/`) — the project lives on GitHub and the `.ai` domain may not be canonical.
